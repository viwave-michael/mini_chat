<% if !@comment || @comment.errors.any? %>
  alert('Your comment cannot be saved.');
<% else %>
  $('#comment_body').val('');
  $('#comments').find("input[type='submit']").val('Submit').prop('disabled', false)
<% end %>